pipeline {
    agent any

    stages {
        stage('Git clone') {
            steps {
                sh 'echo cloning repo'
            }
        }
        stage('Compiling') {
            steps {
                sh ' echo compiling'
            }
        }
    }
}